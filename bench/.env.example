# Supabase (optional — without these the runner logs to ./runs/*.jsonl)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Model providers (only needed for the providers you benchmark)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
